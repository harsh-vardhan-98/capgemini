class Rectangle{
     constructor(height,width){
          this.height = height;
          this.width = width;
     }
}

var box = new Rectangle(10,20);
console.log("height is "+box.height + " width is  " +box.width)